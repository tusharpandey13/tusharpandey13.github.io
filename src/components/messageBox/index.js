import React from "react"

import "./messageBox.scss"

const MessageBox = props => {
  return (
    <div className={`mbox-C`}>
      <span className={`mbox-pic`}>hello</span>
      <span>hello</span>
    </div>
  )
}

export default MessageBox
