const data = require('../public/data.json');

const experience = data['experience'];
const projects = data['projects'];
const aboutme = data['aboutme'];
const links = data['links'];

export { experience, projects, aboutme, links };
